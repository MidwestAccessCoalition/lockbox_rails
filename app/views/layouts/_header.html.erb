<header>
  <div id="main-header">
    <%= render "layouts/nav_bar" %>
  </div>
</header>
<%= render 'layouts/secondary_nav' if current_user.partner? && (current_user.lockbox_partner || @lockbox_partner)&.id %>
